import 'package:flutter/material.dart';

class Vido_call_screen extends StatelessWidget {
  const Vido_call_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child:Container(
          child: Column(
            children: [
              
            ],
          ),
        ) ,
      ),
    );
  }
}
